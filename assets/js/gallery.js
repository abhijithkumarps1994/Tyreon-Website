"use strict";
const galleryFilters=document.querySelectorAll("[data-gallery-filter]");const galleryItems=document.querySelectorAll("[data-gallery-category]");const lightbox=document.querySelector("[data-lightbox]");const lightboxImage=document.querySelector("[data-lightbox-image]");const lightboxCaption=document.querySelector("[data-lightbox-caption]");const closeLightbox=()=>{if(!lightbox)return;lightbox.hidden=true;document.body.style.overflow=""};galleryFilters.forEach((filter)=>filter.addEventListener("click",()=>{const category=filter.dataset.galleryFilter;galleryFilters.forEach((button)=>button.classList.toggle("active",button===filter));galleryItems.forEach((item)=>{item.hidden=category!=="all"&&!item.dataset.galleryCategory.includes(category)})}));galleryItems.forEach((item)=>item.addEventListener("click",()=>{if(!lightbox||!lightboxImage||!lightboxCaption)return;lightboxImage.src=item.dataset.galleryImage;lightboxImage.alt=item.dataset.galleryAlt;lightboxCaption.textContent=item.dataset.galleryCaption;lightbox.hidden=false;document.body.style.overflow="hidden"}));document.querySelector("[data-lightbox-close]")?.addEventListener("click",closeLightbox);lightbox?.addEventListener("click",(event)=>{if(event.target===lightbox)closeLightbox()});document.addEventListener("keydown",(event)=>{if(event.key==="Escape")closeLightbox()});
